public class Main {
    public static void main(String[] args) {
        //Interface: implements -> métodos sem implementação, contém as especificações para outras classes (como um manual base)
        //podem ser implementadas múltiplas interfaces em uma classe
        //todos os métodos da interface são públicos (desnecessário declarar) e de sobrescrita obrigatória na classe que os implementa
        //assinatura dos métodos -> declarar eles na Interface
        //Diferença Classe abstrata com métodos abstratos X Interface
        //Interface NÃO possui implementação de métodos, nem membros (atributos)
        //na herança da classe abstrata, só é possível herdar dela
        //Interface abstrai comportamento, classe abstrata abstrai ... e comportamento

        //UML -> seta pontilhada <Interface>
    }
}